import { RecipeDifficultyPipe } from './recipe-difficulty-pipe';

describe('RecipeDifficultyPipe', () => {
  it('create an instance', () => {
    const pipe = new RecipeDifficultyPipe();
    expect(pipe).toBeTruthy();
  });
});
